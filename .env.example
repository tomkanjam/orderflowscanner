# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: App URL for redirects
VITE_APP_URL=http://localhost:5173

# Optional: Enable/disable Langfuse observability (defaults to true)
VITE_LANGFUSE_ENABLED=true

# Note: Langfuse credentials should be set in Supabase Edge Functions environment
# via the Supabase dashboard or CLI, not in this file:
# - LANGFUSE_SECRET_KEY
# - LANGFUSE_PUBLIC_KEY
# - LANGFUSE_BASE_URL (optional, defaults to https://cloud.langfuse.com)