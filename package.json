{
  "name": "trademind",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm build:clean && pnpm build:app",
    "build:clean": "rm -rf dist",
    "build:app": "pnpm --filter app build",
    "dev:app": "pnpm --filter app dev"
  },
  "devDependencies": {
    "typescript": "~5.7.2"
  }
}
